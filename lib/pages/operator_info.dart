import 'package:flutter/material.dart';

class OperatorInfo extends StatelessWidget {
  const OperatorInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}